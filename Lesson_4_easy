# _Autor_ Karsakova Yulia
import random

# Задание-1:
# Дан список, заполненный произвольными целыми числами.
# Получить новый список, элементы которого будут
# квадратами элементов исходного списка
# [1, 2, 4, 0] --> [1, 4, 16, 0]

list_1 = list(random.randint(0, 10)for _ in range(7)) #создаем массив из чисел
list_2 = [x**2 for x in list_1] #все числа в массиве возводим в квадрат и записываем в новый массив
print(list_2) #выводим на экран

# Задание-2:
# Даны два списка фруктов.
# Получить список фруктов, присутствующих в обоих исходных списках.

sp_1 = ["mango", "apple", "melon", "banana", "orange"]      #создаем список 1
sp_2 = ["apple", "grapes", "lime", "kiwi", "melon", "mango"]   #создаем список 2
result = [_ for _ in (set(sp_1).intersection(sp_2))]      #записываем одинаковые элементы в переменную result
print(result)

# Задание-3:
# Дан список, заполненный произвольными числами.
# Получить список из элементов исходного, удовлетворяющих следующим условиям:
# + Элемент кратен 3
# + Элемент положительный
# + Элемент не кратен 4

spisok = list(random.randint(-50, 50)for _ in range(7))               #создаем список из чисел от (-50) до 50, 7 элементов
spisok_2 = [_ for _ in spisok if _ % 3 == 0 and _ > 0 and _ % 4 != 0] #выводим условие, где (% 3 == 0) - кратен 3, (% 4 != 0) не кратен 4.
print(spisok_2)
