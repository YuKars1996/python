#_autor_ - Карсакова Юлия

# Задание-1:
# Напишите функцию, возвращающую ряд Фибоначчи с n-элемента до m-элемента.
# Первыми элементами ряда считать цифры 1 1

x = input("Введите ряд, который хотите узнать : ")
c = int(x)

def fib(c):
    a = 1
    b = 1
    for i in range(c):
        a, b = b, a + b
    return a
res = str(fib(c))
print(list(res))


# Задача-2:
# Напишите функцию, сортирующую принимаемый список по возрастанию.
# Для сортировки используйте любой алгоритм (например пузырьковый).
# Для решения данной задачи нельзя использовать встроенную функцию и метод sort()

def sort(spisok):
    a = spisok
    for i in range(len(a),0,-1):
        for j in range(1, i):
            if a[j-1] > a[j]:
                tmp = a[j-1]
                a[j-1] = a[j]
                a[j] = tmp
                print (a)
    return a
list = [3, 5, 4, 1, 78, 45, 0, 14]
print (sort(list))


# Задача-3:
# Напишите собственную реализацию стандартной функции filter.
# Разумеется, внутри нельзя использовать саму функцию filter.

x = [2, 5, 78, 1, 45, 0, 31, 26] #Создаем массив из чисел

y = [i for i in x if i != 0 and i > 25]     # функция фильтра: выводим i (каждое число по очереди из массива), если i не равен 0 и i больше 25

print(y)
